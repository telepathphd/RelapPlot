todo:
1.
2.新增棒位和棒速的变化图，不增加输入，而是通过查表反推得到棒位，通过微分得到棒速，这里注意可以警告超过上限的数据；
3.能计算各个图的超调量、调节时间，峰值时间等，输入到文档latex作为参考；
4.打包成exe，在无python的环境下也能使用；
5.能够通过输入-png -pdf -eps确定所要输出的图片类型；
6.能够通过拖拽文件，在对应文件夹下生成输出的图片。

done:
1.上冲下泄、喷淋、电加热均采用脉冲100s的形式引入；
2.使用归一化百分比表示部分值，功率、流量等。
3.legeng的位置不要挡住曲线波动部分（主要是反应性的图）；
4.不同功率水平引入的扰动幅度一致，给水流量变化值一致。
5.更新了.TND文件，新增了给水流量设定值（其归一化后也作为负荷设定值），现在一共有20+10组数据，需要在功率处增加负荷设定值，在给水流量处增加给水流量设定值的曲线；